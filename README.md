# Book Request Management System

A multi-role PHP & MySQL web application that lets users browse books via the **Google Books API**, submit book requests, and allows admins to manage them through dedicated dashboards.
